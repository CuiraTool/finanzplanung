# Anthropic API Key für die Doc-Upload-Funktion (Phase 3).
# Nicht ins Repo committen — nur in .env.local lokal und in Netlify-Env-Variables.
# Key holen: https://console.anthropic.com → Settings → API Keys
ANTHROPIC_API_KEY=sk-ant-api03-...
